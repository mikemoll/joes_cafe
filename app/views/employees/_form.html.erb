<%= form_for @employee, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :full_name, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :full_name, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :possition, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :possition, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  employees_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
