<%= simple_form_for(@employee) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :full_name %>
    <%= f.input :possition %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
